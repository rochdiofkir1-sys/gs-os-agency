# GS-OS v2.0 — Python Dependencies
# All FREE and open-source. No API keys required.
# Python 3.10+ required.

# ── System monitoring (RAM Guard) ─────────────────────────────────────────────
psutil>=5.9.0

# ── HTTP client (Ollama REST + scraping) ──────────────────────────────────────
requests>=2.31.0

# ── Web scraping ──────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0           # Faster HTML parser for bs4 (optional but recommended)

# ── Data handling ─────────────────────────────────────────────────────────────
# stdlib only — json, re, time, pathlib, dataclasses (no extra install needed)

# ── Optional: async support for future parallelism ────────────────────────────
# aiohttp>=3.9.0
# asyncio is stdlib

# ── Optional: richer CLI output ───────────────────────────────────────────────
# rich>=13.0.0

# ── Optional: scheduled runs (cron alternative) ───────────────────────────────
# schedule>=1.2.0

# ═══════════════════════════════════════════════════════════════════════════════
# LOCAL MODELS VIA OLLAMA (all free, no API keys)
# Install once — models live on your disk permanently
# ═══════════════════════════════════════════════════════════════════════════════
#
#   ollama pull llama3.1:8b-instruct-q4_0    (~4.5 GB)  CEO + Architect
#   ollama pull llama3:8b-instruct-q4_0      (~4.5 GB)  COO
#   ollama pull phi3:mini                    (~2.2 GB)  CQO (lightweight validator)
#   ollama pull aya:8b                       (~4.5 GB)  LocalAgent (Arabic)
#
#   Total disk: ~15.7 GB   |   Max RAM at any one time: ~4.5 GB (single-model residency)
#
# ═══════════════════════════════════════════════════════════════════════════════

# ── Document Generation ───────────────────────────────────────────────────────
reportlab>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
# -- Web Backend ---------------------------------------------------------------
Flask>=3.0.0
Flask-Limiter>=3.5.0
gunicorn>=21.2.0
